# Use formatted strings to display a message about a product's price.

# Requirements
# Variable 'price' is 59.
# The variable 'txt' should be a formatted string that displays the price with two decimal places.

# Keep the price like it is
price = 59

# Format the string to include the price with two decimals
txt = f"The price is {price:.2f} dollars"

# Printing the result (keep it like this)
print(txt)
