# Use string indexing with square brackets to print the second character of the string stored in a.

# Requirements
# The second character of the string stored in `a` should be printed.

# Leave this string as it is
a = "Hello, World!"

# Modify the print statement below 
# to only print the second character of the string stored in a
print(a[1]) 