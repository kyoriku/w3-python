# Concatenate, or combine, two strings with a space between them, using the + operator.

# Requirements
# The variable 'a' is 'Hello'.
# The variable 'b' is 'World'.
# The variable 'c' should be the words 'Hello', and 'World', with a space between them.

# Leave the strings as they are
a = "Hello"
b = "World"

# Combine the strings with a space between them, using the + operator
c = a + " " + b

# Leave the print as it is
print(c)