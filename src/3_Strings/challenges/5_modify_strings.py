# Use the initial string "Hello, World!". Use replace() to print the string with "H" replaced with "J".

# Requirements
# Variable `a` is "Hello, World!".
# `replace()` is used to print "Jello, World!".

# Keep this string as it is
a = "Hello, World!"

# Use replace() to print string a, with "H" replaced with "J"
print(a.replace("H", "J"))