# Write a Python program that initializes a list with three specific elements and prints its length using the len() function.

# Requirements
# The list should contain the strings 'apple', 'banana', and 'cherry'.
# The program should print the length of the list.

thislist = ['apple', 'banana', 'cherry']

# Print the length of the list
print(len(thislist))