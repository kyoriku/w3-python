# Add a new item 'orange' to the 'thistuple' tuple.

# Requirements
# Keep the first code line as it is.
# Ensure 'thistuple' includes 'orange'.
# Ensure the length of 'thistuple' is four.

thistuple = ("apple", "banana", "cherry")

# Add the new element and update the tuple
# Write your code here
thistuple = thistuple + ("orange",)

print(thistuple)