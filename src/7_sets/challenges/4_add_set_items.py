# Write a Python program that adds a new item to the set using the add() method.

# Requirements
# Keep the first line as it is.
# The item 'orange' is added to the set.

thisset = {"apple", "cherry"}

# Add an item "orange", using the add() method
thisset.add("orange")

print(thisset)