# Remove the item 'banana' from the given set.

# Requirements
# Keep the first line as it is.
# Remove the 'banana' item from `thisset`.
# The resulting set should contain only 'apple' and 'cherry'

thisset = {"apple", "banana", "cherry"}

# Remove "banana" from the set
thisset.remove("banana")

print(thisset)