# Loop through the given set using a for loop and print each item.

# Requirements
# The set should be defined as {'apple', 'banana', 'cherry'} initially.
# The program should print each element in the set using a for loop.

thisset = {"apple", "banana", "cherry"}

# Loop through the set to print each item
for item in thisset:
  print(item)