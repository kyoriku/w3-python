# Use the Walrus operator (:=)

# Requirements
# `x` must be assigned the value `3`.
# The Walrus operator (`:=`) must be used in the assignment.

# Use the Walrus operator to assign and print the value 3 to variable x
print( x := 3)