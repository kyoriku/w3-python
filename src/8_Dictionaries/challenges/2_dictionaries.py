# Find the length of the dictionary.

# Requirements
# Store the length of the dictionary in the `dictLength` variable, using the `len()` function

thisdict = {
  "brand": "Ford",
  "model": "Mustang",
  "year": 1964,
  "year": 2020
}

# Complete the line below
dictLength = len(thisdict)

print("Dictionary length:",dictLength)