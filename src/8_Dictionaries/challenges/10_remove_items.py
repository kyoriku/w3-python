# Remove all items from the dictionary, using the clear() method.

# Requirements
# Remove all items from `thisdict` ensuring it is empty.

thisdict = {
  'brand': 'Ford',
  'model': 'Mustang',
  'year': 1964
}

# Write code to remove all items
thisdict.clear()

print("Dictionary:",thisdict)