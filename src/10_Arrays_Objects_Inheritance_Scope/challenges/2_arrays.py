# Get the length of the cars array, and assign it to variable 'x'.

# Requirements
# The variable x must be assigned the length of the cars array.
# The length is found using the len() function.

cars = ["Ford","Volvo","BMW"]

#Store the length of the cars array into variable x
x = len(cars)