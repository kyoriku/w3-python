# Remove the second element in the cars array, using the pop() method and the correct index.

# Requirements
# The cars list is defined.
# The only elements left in the cars array are "Ford" and "BMW".
# The pop() method is used.

cars = ["Ford", "Volvo", "BMW"]

# Remove the second element "Volvo", using pop and the correct index
cars.pop(1)