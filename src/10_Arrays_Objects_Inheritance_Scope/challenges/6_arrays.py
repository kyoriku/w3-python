# Remove the 'Volvo' element from the 'cars' array.

# Requirements
# The array 'cars' should no longer contain 'Volvo'.
# Methods `pop()` or `remove()` are used to remove the element.
# The final state of the array 'cars' should only contain 'Ford' and 'BMW'.

cars = ["Ford","Volvo","BMW"]

# Remove "Volvo" from the cars array
cars.remove("Volvo")

print(cars)