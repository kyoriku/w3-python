# Create a Python program with the following:
# add_numbers function: Define a function that takes two arguments and returns their sum.
# multiply_numbers lambda function: Create a lambda function that takes two arguments and returns their product.

# Requirements
# The function `add_numbers` should return the sum of its two arguments.
# The lambda function `multiply_numbers` should return the product of its two arguments.

def add_numbers(a, b):
  # Write your code here
  return a + b

def multiply_numbers(a, b): return a * b